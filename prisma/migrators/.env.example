# Environment variables for migration
# Copy this to .env and update values

# Old Database Connection (Database cũ để import dữ liệu)
OLD_DATABASE_URL="mysql://root:070904@localhost:3307/old_beedatabase"

# New Database Connection (Database mới - đích đến)
DATABASE_URL="mysql://root:070904@localhost:3307/beedatabase"

# Migration Options
MIGRATION_BATCH_SIZE=100
MIGRATION_VERBOSE=true
MIGRATION_DRY_RUN=false
