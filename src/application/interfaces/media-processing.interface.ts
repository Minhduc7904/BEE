/** Application port and Nest injection token for MediaProcessingService. */
export abstract class MediaProcessingService {}

export interface MediaProcessingService {
  optimize(...args: any[]): any
}

