/** Đồng bộ với Prisma enum ActionApprovalRequestStatus. */
export enum ActionApprovalRequestStatus {
  PENDING = 'PENDING',
  PROCESSING = 'PROCESSING',
  ACCEPTED = 'ACCEPTED',
  DECLINED = 'DECLINED',
  CANCELED = 'CANCELED',
  EXPIRED = 'EXPIRED',
}
