// src/shared/enums/device-platform.enum.ts

/**
 * Device Platform Enum
 * Nền tảng của thiết bị đăng ký nhận thông báo đẩy.
 * Đồng bộ 100% với Prisma schema enum DevicePlatform
 */
export enum DevicePlatform {
  ANDROID = 'ANDROID',
  IOS = 'IOS',
}
